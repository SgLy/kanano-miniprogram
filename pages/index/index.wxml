<search-box
  height="{{ navigationBarHeight }}"
  atTop="{{ atTop }}"
/>
<view style="padding-top: {{ navigationBarHeight }}px" >
  <!--<annotator parsedText="{{ parsed }}" />-->
  <card
    wx:for="{{ cards }}"
    wx:forindex="index"
    wx:key="{{ index }}"
    createdTime="{{ item.createdTime }}"
    text="{{ item.text }}"
  ></card>
</view>